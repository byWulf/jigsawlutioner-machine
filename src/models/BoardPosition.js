export default class BoardPosition {
    /**
     * @type {int}
     */
    x = 0;

    /**
     * @type {int}
     */
    y = 0;

    constructor(x, y) {
        this.x = x;
        this.y = y;
    }
}
